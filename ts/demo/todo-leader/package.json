{
  "name": "@voidmerge/todo-leader",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --include-workspace-root -w",
    "test": "npm test --include-workspace-root -w",
    "start": "npm run build && vm test --code-file dist/bundle-todo-code.js --code-env package.json"
  },
  "dependencies": {
    "@noble/ed25519": "^3.0.0",
    "@voidmerge/voidmerge-client": "^0.0.14",
    "@voidmerge/voidmerge-code": "^0.0.14"
  },
  "devDependencies": {
    "base64-js": "^1.5.1"
  }
}
